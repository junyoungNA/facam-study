import React from 'react'

const Input = ({
    id,
    label,
    type = 'text',
    disabled,
    formatPrice,
    register,
    required,
    erros,
}) => {
    return (
        <div>
        
        </div>
    )
}

export default Input
