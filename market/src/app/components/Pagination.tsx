import React from 'react'

interface PaginationProps {
    page:number;
    totalItems: number;
    perPage : number;
      
}

const Pagination = ({
    page,
    totalItems,
    perPage,
} : PaginationProps) => {
    return (
        <div>
        
        </div>
    )
}

export default Pagination
